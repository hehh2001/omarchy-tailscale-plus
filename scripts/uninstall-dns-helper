#!/usr/bin/env bash
set -euo pipefail

sudo rm -f /etc/sudoers.d/omarchy-tailscale-plus-dns
sudo rm -f /usr/local/libexec/omarchy-tailscale-plus-dns
echo "Removed the Tailscale Plus DNS helper and sudo rule."
